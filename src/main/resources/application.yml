server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Book
    username: junhyuk919 # 본인 MySQL 계정 ID
    password: ${DB_PASSWORD} # 본인 MySQL 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

odsay:
  api:
    odsay_key: ${ODSAY_API_KEY}
    KAKAO_REST_API_KEY: ${KAKAO_REST_API_KEY}
